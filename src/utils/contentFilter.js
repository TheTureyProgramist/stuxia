export const BANNED_PATTERNS = [
  // Війна та конфлікти
  /війн(?:а|и|ою|і)/ui,
  /бойов(?:і|их|им|ий|а)/ui,
  /конфлікт(?:у|и|ів|ом)?/ui,
  /фронт(?:у|і|ом)?/ui,
  
  // Політика
  /політик(?:а|и|ою|і)/ui,
  /корупці(?:я|ї|ю|єю)/ui,
  /депутат(?:и|ів|ом|а)?/ui,
  
  // Кримінал та злочини
  /кримінал(?:у|ом)?/ui,
  /суд(?:у|ом|ів)?/ui,
  /затриман(?:о|ий|і|а)/ui,
  /вбивств(?:о|а|ом|ах)/ui,
  /крадіжк(?:а|и|ою|ах)/ui,
  /вирок(?:у|и|ів|ами|ах)?/ui,
  /в'?язниц(?:я|і|ю|ею|ях)/ui,
  /арешт(?:у|и|ів|ами|ах)?/ui,
  /теракт(?:и|ів|ами|ах|у)?/ui,
  /вибух(?:и|ів|ами|ах|у)?/ui,
  /злочин(?:у|и|ів|ом)?/ui,

  // Adult / Небажаний контент
  /18\+/i,
  /порно/ui,
  /секс(?:у|ом|уальн.*)?/ui,
  /еротик(?:а|и|ою|і)/ui,

  // Азартні ігри
  /казино/ui,
  /ставк(?:а|и|ами|ах)/ui,
  /рулетк(?:а|и|ою|ці)/ui,

  // Релігія та містика
  /релігі(?:я|ї|ю|єю)/ui,
  /масон(?:и|ів|ами|ах)?/ui,
  /сект(?:а|и|ою|ами|ах)/ui,
  
  // Інше (з попереднього списку)
  /рецепт(?:и|ів|ами|ах|у)?/ui
];

/**
 * Нормалізація тексту: заміна латинських літер на кириличні аналоги (для захисту від обходу),
 * видалення зайвих пробілів і приведення до нижнього регістру.
 */
export const normalizeText = (text) => {
  if (!text) return "";
  const charMap = {
    'a': 'а', 'c': 'с', 'e': 'е', 'i': 'і',
    'o': 'о', 'p': 'р', 'x': 'х', 'y': 'у'
  };
  
  return text.toLowerCase().replace(/[aceiopxy]/g, match => charMap[match]);
};

/**
 * Перевіряє текст на наявність забороненого контенту за допомогою Regex та нормалізації.
 * @param {string} text - Текст для перевірки
 * @returns {boolean} - true, якщо знайдено заборонений контент
 */
export const hasBannedContent = (text) => {
  if (!text) return false;
  const normalized = normalizeText(text);
  return BANNED_PATTERNS.some((pattern) => pattern.test(normalized));
};
